var request = require('http');
var usersList = require('../lib/users');

describe('Derp-BearAngular Routes', function () {
    it('returns a list of users', function(){
        usersList
    });

});
